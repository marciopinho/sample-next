// in the example, `localhost:3000/cars` would show the full list of cars, while
// localhost:3000/cars/1 would show the details for the car with id 1.
// for this we needa a [param].tsx file in the cars folder, to make the ROUTE DYNAMIC.

export default function Car() {
  return <h1>List of cars</h1>
}